apiVersion: v1
kind: Service
metadata:
  name: nba-backend-svc
  namespace: nba
  labels:
      app: nba-backend
spec:
  type: NodePort
  selector:
    app: nba-backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
