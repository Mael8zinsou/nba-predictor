apiVersion: v1
kind: Service
metadata:
  name: nba-frontend-svc
  namespace: nba
spec:
  type: NodePort
  selector:
    app: nba-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
